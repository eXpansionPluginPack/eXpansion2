expansion_join_leave_messages:
  connect: '|server| Connection |success| {default}%group% %nickname% {default}({muted}%login%{default}) joins.'
  disconnect: '|server| Disconnect |success| {default}%group%, %nickname% {default}({muted}%login%{default}) leaves.'
