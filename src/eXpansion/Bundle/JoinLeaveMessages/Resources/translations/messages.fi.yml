expansion_join_leave_messages:
  connect: '|server| {default}%group% %nickname% {default}({muted}%login%{default}) from {variable}%path% {default}joins.'
  disconnect: '|server| {default}%group% %nickname% {default}({muted}%login%{default}) from {variable}%path%  {default}leaves.'
  applicationGreeter: '|server| {info}Welcome! This server uses {variable}eXpansion² v%version%{info} to manage UI and Records.'
