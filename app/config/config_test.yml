imports:
    - { resource: config_dev.yml }
    - { resource: expansion.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

swiftmailer:
    disable_delivery: true
